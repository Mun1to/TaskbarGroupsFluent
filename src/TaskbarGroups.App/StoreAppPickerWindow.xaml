<ui:FluentWindow x:Class="TaskbarGroups.App.StoreAppPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Apps de Microsoft Store"
        Height="640" Width="500"
        MinHeight="480" MinWidth="420"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Apps de Microsoft Store" Padding="12,6" />

        <StackPanel Grid.Row="1" Margin="20,4,20,12">
            <TextBlock Text="Selecciona apps de la Store"
                       FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="Apps sin .exe (WhatsApp, Instagram, TikTok…) que el explorador no puede anclar"
                       FontSize="12" Margin="0,2,0,10"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap" />
            <ui:TextBox x:Name="SearchBox" PlaceholderText="Buscar app…"
                        Icon="{ui:SymbolIcon Search24}"
                        TextChanged="SearchBox_TextChanged" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="20,0,20,0">
            <ui:ProgressRing x:Name="LoadingRing" IsIndeterminate="True"
                             Width="40" Height="40"
                             HorizontalAlignment="Center" VerticalAlignment="Center" />
            <ListBox x:Name="AppsList"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8,6" Margin="0,2" CornerRadius="8">
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}">
                                <CheckBox.Content>
                                    <Grid Margin="6,0,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Width="32" Height="32"
                                                CornerRadius="6"
                                                Background="{DynamicResource ControlFillColorSecondaryBrush}">
                                            <Image Source="{Binding Icon}" Width="28" Height="28"
                                                   RenderOptions.BitmapScalingMode="HighQuality" />
                                        </Border>
                                        <TextBlock Grid.Column="1" Text="{Binding DisplayName}"
                                                   VerticalAlignment="Center" Margin="12,0,0,0"
                                                   TextTrimming="CharacterEllipsis"
                                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                    </Grid>
                                </CheckBox.Content>
                            </CheckBox>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Border Grid.Row="3" Padding="20,12"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="0,1,0,0">
            <Grid>
                <TextBlock x:Name="CountText" Text="0 seleccionadas"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Content="Cancelar" Margin="0,0,8,0" Click="Cancel_Click" />
                    <ui:Button x:Name="AddButton" Content="Añadir" Appearance="Primary"
                               Icon="{ui:SymbolIcon Add24}" Click="Add_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
