<ui:FluentWindow x:Class="TaskbarGroups.App.EmojiPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="{DynamicResource Loc_Emoji_Title}"
        Height="560" Width="460"
        MinHeight="420" MinWidth="380"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterOwner">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{DynamicResource Loc_Emoji_Title}" Padding="12,6" />

        <StackPanel Grid.Row="1" Margin="20,4,20,8">
            <TextBlock Text="{DynamicResource Loc_Emoji_Heading}"
                       FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="{DynamicResource Loc_Emoji_Subtitle}"
                       FontSize="12" Margin="0,2,0,0" TextWrapping="Wrap"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="12,0,12,0">
            <ui:ProgressRing x:Name="LoadingRing" IsIndeterminate="True"
                             Width="40" Height="40"
                             HorizontalAlignment="Center" VerticalAlignment="Center" />
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="EmojiItems">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Tag="{Binding Emoji}" Click="Emoji_Click"
                                    Width="52" Height="52" Margin="3" Padding="0"
                                    Background="Transparent" BorderThickness="0"
                                    Cursor="Hand" ToolTip="{Binding Emoji}"
                                    AutomationProperties.Name="{Binding Emoji}">
                                <Image Source="{Binding Image}" Width="34" Height="34"
                                       RenderOptions.BitmapScalingMode="HighQuality" />
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <Border Grid.Row="3" Padding="20,12"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                BorderThickness="0,1,0,0">
            <ui:Button Content="{DynamicResource Loc_Common_Cancel}"
                       HorizontalAlignment="Right" Click="Cancel_Click" />
        </Border>
    </Grid>
</ui:FluentWindow>
